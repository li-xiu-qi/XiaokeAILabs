# 代码与表格的相似度：embedding 对结构化内容的分辨力

单个 notebook 的专项实验：普通文本 embedding 模型遇到代码块和 Markdown 表格时还管不管用。用同一套嵌入模型分别编码代码、表格和自然语言，两两算余弦相似度，回答「RAG 系统里混着代码和表格的文档，检索会不会错配」。

结论（嵌入模型对结构化内容的分辨力有限）已并入 `test_text_segmentation` 的选型边界，这里保留原始实验过程与可视化。两个 notebook 共享同一套实验框架（获取嵌入、算相似度、画热力图），改模型路径和数据定义即可复用。
