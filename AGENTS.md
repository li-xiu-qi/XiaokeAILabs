# XiaokeAILabs

公开的 AI 工程实验仓库（Apache 2.0）。`experiments/` 下每个子目录是一个自包含的技术点实验，兼作公众号「筱可AI」文章的可复运行配套代码。本仓服务学习、教学与实验验证，不是生产项目。

## 仓库结构

```
XiaokeAILabs/
├── experiments/          全部实验，每个目录自包含（依赖、数据说明、文档、脚本自成一套）
├── utils/                跨实验复用的纯工具函数
├── images/               公众号素材配图
├── main.py               仅入口问候，无统一运行入口
└── requirements.txt      只放开发工具，不放运行时依赖
```

根 `requirements.txt` 不放任何运行时依赖，这是硬约束：新增实验时，依赖写在实验目录自己的 requirements 或 README 复现步骤里。实验目录之间不互相 import，删一个实验不留残依赖。

## 实验目录内部约定

- `README.md` 是入口：写清测什么、不测什么，被测对象的名称与版本、数据来源、复现命令齐全
- `scripts/` 放可复现脚本，`docs/` 放专题文档，`reports/` 放分阶段报告（大实验才有）
- 报告命名必须体现数据 regime：用合成、随机、失真数据的报告，文件名里标明，不与真实数据报告同名系
- 每份报告开头声明职责：测了什么、没测什么、后续工作该不该引用它
- 失真或探底数据单独成档，不与主报告混在同一目录层级
- 测量陷阱与踩坑记录在 `docs/` 单独立文，不埋没在报告正文段落里
- 复现命令里的 Python 解释器写 `python` 或实验自带 `./.venv/...`，不写任何本机绝对路径

## 内容分流：什么留仓，什么迁出

实验仓只放这个实验独有的东西：代码、配置、数据说明、与本次机器/数据/版本绑定的报告、迭代日志、踩坑记录。

以下三类迁出，仓内只留摘要加纯文字指针：

1. 可迁移知识：公式推导的通用形态、算法与结构原理、方法论、跨硬件成立的成本模型结论
2. 外部调研综述：工具与公司背景、横向横评、源码级对比
3. 选型结论：服务未来技术决策的对比结论（用哪个库、哪种索引、什么量化）

判据一句话：换掉这台机器、这份数据、这个版本，这段内容还成不成立。成立的是知识，迁出；不成立的才是本实验的证据，留下。

迁出去向是配套的个人知识库与私有档案仓（均不随本仓发布）。未定稿、涉密或仅探索中的实验不进本仓，进私有实验区；已完结的冷档案进归档仓。指针格式用纯文字指引（完整案例：某私有仓/某路径），不维护跨仓可点击链接。本仓公开，迁出的知识常含本机路径与内部仓名，反过来也放不进本仓，分流是硬约束不是偏好。

## 开发纪律

- pre-commit：black（line-length 100）加 trailing-whitespace、end-of-file-fixer、check-yaml、check-added-large-files，提交前安装并运行
- 命名：实验目录 `test_` 前缀是单点验证，无前缀是可运行小应用；代码与脚本用英文，文档用中文名
- commit：Conventional Commits，一个 commit 一个语义单元；逐文件 `git add`，禁用 `git add -A` 与 `git commit -a`
- 措辞合规：代码注释、commit message、公开文档不写内部系统路径、私有仓名、本机环境细节；被测产品实名出现允许（它们是被测对象），但不描述本仓在对齐或复刻任何内部项目
- 产物不入库：`models/`、`.venv/`、`results/` 已由 `.gitignore` 覆盖；新增同类产物继续加 ignore，二进制与数据文件不进版本库

## 规范文件分层

本文件随仓公开，是对外规范，只放活约束与稳定结构。内部协作信息（本仓在个人工作体系中的位置、与私有仓的分流细则、归档位置）维护在仓内私有层 `.step-code/AGENTS.md`，该文件被 git 排除、不随仓发布。进入本仓干活前两份都读。

## 维护说明

- 实验清单不手写，以 `experiments/` 目录与 README 索引为准
- 复盘与实测流水不进本文件，下沉到对应实验的 `docs/` 或私有层
